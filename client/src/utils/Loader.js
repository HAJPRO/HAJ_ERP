import { useLoading } from 'vue-loading-overlay'
export const loading = useLoading({
    color: "#36d887",
    backgroundColor: "#666",
    opacity: "0.9",
    loader: "bars",
    height: "80px",
    width: "80px",
});